# Portfolio Assets TODO

The following placeholder assets are needed to complete the portfolio build:

- [ ] **Favicon**: Add a custom `/vite.svg` or `/favicon.ico` in the `public` directory.
- [ ] **OG Image**: Generate an Open Graph image for social sharing and add it to `public/og-image.jpg`.
- [ ] **Project Thumbnails / Videos**:
  - `X-NCU`: Add an image or looping video (e.g., `x-ncu-preview.mp4`). Update `ProjectCard` component to load this if available.
  - `Project Sera`: Add an image or looping video (e.g., `sera-preview.mp4`).
- [ ] **Fonts**: Host and add `JetBrains Mono` and a primary Sans (`Roobert` equivalent like `Inter` or `Söhne`) to `index.html` or import via CSS if they aren't available locally. (Currently relying on system fallbacks and standard Google Fonts loading if added).
